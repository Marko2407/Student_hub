package com.mvukosav.studenthub.network

enum class ErrorCode {
    NOT_FOUND,
    INSUFFICIENT_PERMISSION,
    FORBIDDEN,
    AUTHORIZATION_ERROR,
    UNKNOWN_ERROR,
    NETWORK_ERROR,
    USER_NOT_FOUND
}